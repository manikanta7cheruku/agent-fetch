# agent/planning.py
"""
Phase 2 placeholder: agent logic.

In the future, this module will:
- Accept natural language goals from users,
- Use an LLM to plan which tools to call (get_weather, get_crypto_price),
- Set up schedules and alerts in the scheduler module,
- Generate human-readable summaries.

Example future function:
    def handle_instruction(user_id: str, text: str) -> str: ...
"""